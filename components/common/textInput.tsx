import { TextInput } from 'react-native-paper'
import { TextInputProps } from 'react-native-paper/lib/typescript/components/TextInput/TextInput'

export const TextInputComp = (props: any) => {
    return <TextInput {...props}></TextInput>
}